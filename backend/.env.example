# =============================================================================
# LEGAL AI SYSTEM - BACKEND ENVIRONMENT CONFIGURATION (EXAMPLE)
# =============================================================================
# IMPORTANT: The backend loads environment variables from the ROOT .env file,
# NOT from backend/.env
#
# All environment variables should be placed in:
#   /.env  (project root)
#
# This file is kept for reference only. See root .env for actual configuration.
# =============================================================================
